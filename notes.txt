A packet is the following:
    [NetworkPacket]
    name: string,
    timestamp: SystemTime,
    raw: vec<u8>
    layers: vec<layer>

    [Layer]
    Contains the layer type, name and fields.
    For TCP this would be OsiLayer::Transport, name: "TCP Packet", and TCP fields
    Each layer of the OSI Model should make a layer object

    [Field]
    A field with name and value

Architecture:
    Packet capture
        Channel
    Pipeline
        Layer 2-4 Protocol
        Application Layer
    UI

    Packet registry
    Reassembly registry
    Packet Context
    ParseOutcome

[TcpStream]
src: (IpAddr, u16),
dst: (IpAddr, u16),
buffer: BTreeMap<u32, Vec<u8>, tree of all the segments of the TCP stream

[IpFragmentedPacket]
id: u16,
source: IpAddr,
dst: IpAddr,
protocol: u8,
fragments: BTreeMap<u16, Vec<u8>>

[Parser trait]
fn parse(&self, data: &[u8]) -> Option<Layer>

[Pipeline]
Capture loop -> parser -> reassembler
Use channel (tx, rx)
Capture loop -> tx -> rx -> parser -> reassembler

[Done]
Ethernet Frame
IP Packet
TCP Packet
Seperate layers into "handlers/files"
Reassemble packet fragments
    Index by 4-tuple object
Make UI look better
Handle IPv6
    For extensions, parse each extension in order
Parse Tcp Flags
Make a custom parsing library for layer 5-7(wip)

[Todo next]
    Replace UUID with pure integer ID
    Find some way to decide what the info field should show. It can be information about the top level packet, or if its an IP fragment, the link to the defragmented frame
    ** Handle TCP segments **
    Filtering logic, like what to ignore (Icmp)
    Fix defragmented packet
        Fragment ID

    Give ability to follow a TCP Stream (stream ID)
        Use both IP-address as well as TCP Port
        Handle IP and TCP port for both Client and Server
    Same for UDP
    Make IPv6 parsing better?
    Parse ICMP codes and types

Warn if checksums are wrong

[Thoughts]
** Fragment Manager ? **
Detect fragment overlapping and check for fragment completeness (not my job? but could warn user)
Think about how to structure a "flow", or packets that belong together
4-tuple struct for easy comparison?
** Pipeline object to define a custom pipeline of layer parsers? **


[Protocol Dissector]
Make each dissector a Lua script that can be loaded as a plugin
Naming convention: <protocol>_dissector.lua, e.g.: HTTP_dissector.lua

[TCP segment]
Sequence number
